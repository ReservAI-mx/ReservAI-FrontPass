name: Validate and Deploy

on:
  push:
    branches:
      - main

concurrency:
  group: deploy-master
  cancel-in-progress: false

jobs:
  validate:
    name: Validate frontend
    runs-on: ubuntu-latest
    environment: prod
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Audit dependencies
        run: npm audit

      - name: Check required files
        run: |
          for f in Dockerfile fly.toml package.json server.js; do
            if [ ! -f "$f" ]; then
              echo "::error::Falta el archivo requerido: $f"
              exit 1
            fi
          done
          grep -q 'app = "passmanager-front"' fly.toml \
            || { echo "::error::fly.toml no apunta a passmanager-front"; exit 1; }

      - name: Syntax check
        run: |
          node --check server.js
          find . -name '*.js' -not -path './node_modules/*' -print0 \
            | xargs -0 -n1 node --check

  deploy:
    name: Deploy to Fly.io
    needs: validate
    runs-on: ubuntu-latest
    environment: prod
    env:
      FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check FLY_API_TOKEN
        run: |
          if [ -z "$FLY_API_TOKEN" ]; then
            echo "::error::Falta FLY_API_TOKEN en el environment 'prod'."
            echo "::error::Settings → Environments → prod → Environment secrets."
            echo "::error::Genera el token con: fly tokens create deploy -a passmanager-front -x 999999h"
            exit 1
          fi

      - name: Setup flyctl
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Deploy
        run: flyctl deploy --remote-only
